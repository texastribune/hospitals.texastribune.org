<% root_url = data.sitemap.url %>
<ul>
  <% sitemap.resources.each do |page| %>
    <% unless page.url =~ /\./ || page.url =~ /hospital/ %>
      <li><a href="<%= "#{root_url}#{page.url}" %>"><%= "#{root_url}#{page.url}" %></a></li>
    <% end %>
  <% end %>
</ul>
